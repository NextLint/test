import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  root: {
    height: '122px',
    width: '410px',
  },
  icon: {
    fontSize: '90px',
    color: '#e1e7eb',
  },
}));
